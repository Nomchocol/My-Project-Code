using UnityEngine;

public class TakeColliderDamage : MonoBehaviour
{
    /*void OnTriggerEnter(Collider other)
    {
        Debug.Log("ชนกับ " + other.name);

        if (other.CompareTag("Damage"))
        {
            FindFirstObjectByType<TakeDamage>().takeDamage();
        }
    }*/

    /*private void OnCollisionEnter(Collision collision)
    {
        Debug.Log("ชนกับ " + collision.gameObject.name);

        TakeDamage takeDamage = GetComponentInParent<TakeDamage>();

        if (takeDamage != null)
        {
            takeDamage.takeDamage();
        }
    }*/

}
